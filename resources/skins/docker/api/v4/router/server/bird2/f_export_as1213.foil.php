
            # Filter for Facebook as per INEX ticket #12919
            #export filter {
            #    #if (bgp_path.first = 6939 ) then {
            #    if (bgp_path.first = 112 ) then {
            #        reject;
            #    }

            #    accept;
            #};
